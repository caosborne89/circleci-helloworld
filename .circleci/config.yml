version: 2
jobs:
  build:
    docker:
      - image: debian:stretch
    steps:
      - checkout
      - run:
          name: Greeting
          command: echo hello world again.
      - run:
          name: Print the Current Time
          command: date